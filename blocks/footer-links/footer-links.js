export default async function decorate(block) {
  // The footer-links block has one row with 4 cells (columns).
  // Each cell contains an h6 heading + list of paragraph links.
  // No restructuring needed — CSS grid handles the layout.
}
